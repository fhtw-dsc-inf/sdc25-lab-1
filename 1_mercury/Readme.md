markdown
# How to Use Mercury

## Installation
1. Install Mercury via pip:
   ```bash
   pip install mercury-framework
   ```

## Running the Application
1. Navigate to the directory containing your Jupyter Notebook files.
2. Execute the command to run Mercury:
   ```bash
   mercury run
   ```
3. Access the interactive dashboard by navigating to the URL displayed in your terminal.